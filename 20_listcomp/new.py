#! /usr/bin/python3
# RASHA:
import sys
from functools import reduce

def main(file):
    infile= open(file,'r')
    lines= infile.read().split('\n')# infile is now a list of lines
    lines = join

def findFreq(word):
    lst= main('text.txt')
    return reduce(lambda x,y: print(type(x)), lst)

print(findFreq("hi"))
